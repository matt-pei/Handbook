apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: oso-task-int
  name: oso-task-int
  namespace: hdms-app
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: oso-task-int
  template:
    metadata:
      labels:
        k8s-app: oso-task-int
      name: oso-task-int
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - oso-task
            topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: oso-task-int
      containers:
      - env:
        - name: PROFILES
          value: ningxia-pro
        - name: CONSUL_IP
          value: http://10.9.4.86
        - name: CONSUL_PORT
          value: "8500"
        - name: JAVA_OPTS
          value: -Xmx4000m -XX:MetaspaceSize=256m -XX:+TieredCompilation -Dspring.config.use-legacy-processing=true
        - name: WORKER
          value: oem
        image: 388144527693.dkr.ecr.cn-northwest-1.amazonaws.com.cn/hdms/oso-task-int:22q4sp1-v0-220620
        imagePullPolicy: Always
        name: oso-task-int
        ports:
        - containerPort: 8787
        resources:
          limits:
            cpu: 1000m
            memory: 4000Mi
          requests:
            cpu: 1000m
            memory: 4000Mi
        securityContext:
          privileged: false
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 30


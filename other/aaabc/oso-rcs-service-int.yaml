apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    k8s-app: oso-rcs-service-int
  name: oso-rcs-service-int
  namespace: hdms-app
spec:
  replicas: 2
  selector:
    matchLabels:
      k8s-app: oso-rcs-service-int
  strategy:
    rollingUpdate:
      maxSurge: 30%
      maxUnavailable: 50%
    type: RollingUpdate
  template:
    metadata:
      labels:
        k8s-app: oso-rcs-service-int
      name: oso-rcs-service-int
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - oso-rcs-service-int
            topologyKey: "kubernetes.io/hostname"
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: oso-rcs-service-int
      containers:
      - env:
        - name: PROFILES
          value: ningxia-pro-int-test
        - name: CONSUL_IP
          value: http://10.9.4.86
        - name: CONSUL_PORT
          value: "8500"
        - name: JAVA_OPTS
          value: -Xmx8000m -XX:MetaspaceSize=512m -XX:+TieredCompilation -Dspring.config.use-legacy-processing=true
        image: 388144527693.dkr.ecr.cn-northwest-1.amazonaws.com.cn/hdms/oso-rcs-service-int:22q3sp1-v0-220426
        imagePullPolicy: Always
        name: oso-rcs-service-int
        ports:
        - containerPort: 8785
        resources:
          limits:
            cpu: 4000m
            memory: 8000Mi
          requests:
            cpu: 4000m
            memory: 8000Mi
        securityContext:
          privileged: false
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 30


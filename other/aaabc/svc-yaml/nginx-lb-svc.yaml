---
apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: nginx-lb
  name: nginx-lb
  namespace: hdms-app
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    k8s-app: nginx-lb
  type: NodePort

